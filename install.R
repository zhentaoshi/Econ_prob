# Run whatever R code before the image is loaded
# for example, install or load packages

# For short commands, use `RUN R -e "r_code"`
# For long commands, write in this file


# install.packages(c("magrittr","readr", "dplyr", "plyr")) # all these packages are included in rocker/bindr



